
<%= render "shared/user_header" %>

<div class = "container">

	<div class= "row">
  		<div class="col-lg-4">

        <div>
          <mark style="font-size: 20px;">打刻詳細</mark>
        </div>

	<table class="table">
      <tbody>
        <tr>
         <th>現場名</th>
         <td><%= @attendance_record.construction_site.name %></td>
       </tr>

       <tr>
         <td>打刻時間</td>
         <td><%= @attendance_record.start_time.strftime("%Y-%m-%d %H:%M") %> 〜 <%= @attendance_record.finish_at.strftime("%Y-%m-%d %H:%M") %></td>
       </tr>

    </tbody>
  </table>
  <%= link_to "編集する",edit_users_attendance_record_path, class: 'btn btn-info btn-lg col-lg-offset-12' %>
</div>