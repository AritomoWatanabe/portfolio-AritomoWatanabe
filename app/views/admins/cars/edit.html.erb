
<%= render "shared/admin_header" %>

<div class = "container">

  <% if @car.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h4 class="alert-heading"><%= pluralize(@car.errors.count, "error") %></h4>
      <p><%= pluralize(@car.errors.count, "error") %> prohibited this object from being saved: not successfully</p>
      <hr>
        <p class="mb-0">
        <% @car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </p>
    </div>
    <% end %>


	<div class= "row">

  		<div class="col-xs-6">

  			<div class="title">車両名変更</div>

			<%= form_for @car, url: admins_car_path do |f| %>

			<div class="field form-group">
        		<%= f.label :name, "車両名" %>
        		<%= f.text_field :name, class: "form-control" %>
      		</div>

      		<div class="btn1">
      			<%= f.submit "車両名変更", class: "btn btn-primary" %>
      		</div>

      		<% end %>

  		</div>

  	</div>

</div>